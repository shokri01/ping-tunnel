# نصب با یک دستور

## سمت سرور خارج
```bash
bash <(curl -fsSL https://raw.githubusercontent.com/<USER>/<REPO>/main/ir-tunnel.sh) kharej
```

## سمت سرور ایران (بعد از بردن فایل اعتبارنامه)
```bash
bash <(curl -fsSL https://raw.githubusercontent.com/<USER>/<REPO>/main/ir-tunnel.sh) iran /root/reality-credentials.txt
```
